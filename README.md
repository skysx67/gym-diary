# Дневник зала — Telegram Mini App + бот

Мини-приложение для Telegram: дневник тренировок в зале.

**Концепция:**
- **Календарь-лента дней** (сегодня по центру, прокрутка вперёд/назад). Дни с тренировкой отмечены точкой.
- **Свои тренировки-шаблоны** (вкладка «Тренировки»): создаёшь тренировку = список упражнений.
- Тап по дню → **выбор тренировки** → в карточке дня вписываешь на каждое упражнение вес,
  количество подходов (степпер −/+) и повторения в каждом подходе.
- **Вес по подходам**: основной вес — общий, но под каждым подходом есть «＋ вес», чтобы задать свой
  (20 → 20 → 22). ✕ возвращает общий. В графике прогресса берётся рабочий вес — максимум из общего
  и весов подходов. Подсказка прошлого раза при разных весах выглядит как `12×40 / 10×40 / 8×44`.
- **Обмен тренировками**: «↗ поделиться» выдаёт код программы, друг вставляет его через
  «⬇ Добавить по коду». Сервера нет, поэтому обмен идёт текстом — код можно переслать в Telegram
  кнопкой «Отправить в Telegram». Импорт всегда создаёт новые id, чужие данные не смешиваются
  с твоими. Ссылка вида `?startapp=<код>` (или `?w=<код>` в браузере) сразу открывает подтверждение.
- **Суперсеты**: в редакторе тренировки кнопка 🔗 сцепляет упражнение с предыдущим (или сразу
  «＋ суперсет» — добавляет пару). Сцепленные упражнения идут одним блоком с подписью «Суперсет»
  и в списке тренировки, и в карточке дня. В группе может быть 2, 3 и больше упражнений.
- **Перенос веса**: при назначении тренировки на новый день вес подтягивается с прошлого раза,
  под упражнением подсказка «Прошлый раз (дата): 60 кг · 10/10/9».
- **Заметки к упражнениям**: в карточке упражнения «＋ Заметка на следующий раз» сохраняет короткое
  замечание (например, «повысить вес на 2,5 кг»). На следующей тренировке оно показывается отдельным
  блоком «Заметка с прошлого раза» и не копируется дальше автоматически.
- **Статистика** (вкладка «Статистика»): график роста весов «горой» (area+line). Режимы —
  по упражнению (вес по датам) и по тренировке (среднее + медиана по всем упражнениям). Плитки:
  последний, максимум, рост, медиана, повторения. Ниже — история по датам: вес **и повторения**
  (`42,5 кг · 12/10/10`, а при разных весах подходов — `12×44 / 10×47,5`).
  Дробные веса не округляются: вес можно вводить и через запятую (`42,5`), и через точку.
- **Синхронизация между устройствами** через Telegram CloudStorage (привязано к твоему аккаунту,
  без бэкенда). У каждого пользователя своё облако — можно давать друзьям.
- **Дизайн** — тёмно-синий + белый в духе NN Fitness. Тап по пустому месту закрывает клавиатуру.

Всё в одном файле `index.html` (без сборки). Данные: `localStorage` (локальный кэш) + Telegram
CloudStorage (синхронизация, только внутри Telegram).

## Файлы
- `index.html` — само мини-приложение.
- `bot.js` — бот: ставит кнопку «Открыть» и отвечает на `/start` (Node.js, без зависимостей).
- `package.json` — запуск бота (`npm start`).

## Живая версия
- Приложение: https://skysx67.github.io/gym-diary/ (GitHub Pages)
- Бот: @dnevnik_spuchenka_bot — кнопка «Открыть» слева снизу запускает мини-приложение.

Обновление: любой пуш в `main` → GitHub Pages пересобирается за пару минут сам.

## Модель данных (localStorage ключ `gymDiary.v3`)
```
workouts:   [ { id, name, exercises: [ { id, name, ss } ] } ]    // шаблоны; ss = id группы суперсета
workoutsAt: 1753000000000                                        // метка времени для слияния
sessions: {
  "YYYY-MM-DD": {
    workoutId,                                                   // выбранная тренировка
    at,                                                          // метка времени последней правки
    exercises: [ { id, name, ss } ],                             // снимок на момент назначения
    entries: { <exId>: {
      weight,                                                    // общий вес упражнения
      reps: [ ... ],                                             // повторения по подходам
      wts:  [ ... ],                                             // свой вес подхода: null = общий
      note                                                        // заметка, которая покажется в следующий раз
    } }
  }
}
```
Подряд идущие упражнения с одинаковым `ss` — один суперсет.

**CloudStorage:** ключ `workouts` + по ключу на день `s_YYYY-MM-DD` (инкрементальная синхронизация).
⚠️ Telegram разрешает в ключах только `A-Z a-z 0-9 _ -`. Ключ с любым другим символом (например с
двоеточием) клиент **молча отбрасывает и не вызывает колбэк** — поэтому charset проверяется в коде
(`KEY_RE`), а на каждую пачку операций и на загрузку повешен watchdog.

**Слияние, а не затирание.** При старте локальное состояние объединяется с облачным: побеждает более
свежая версия по метке `at`, при равных метках — та, где заполнено больше упражнений. Дни, которых нет
в облаке, не удаляются, а доливаются наверх. Плюс локальная резервная копия `gymDiary.v3.bak`, из
которой состояние поднимается, если основной ключ пуст или повреждён.

История каждого дня — снимок упражнений, поэтому правка шаблона не переписывает прошлые записи.
Сегодняшний и будущие дни состав шаблона подтягивают автоматически (цифры привязаны к id и не теряются).

## Бот (опционально, для приветствия на /start)
Кнопка «Открыть» работает без запуска кода (она хранится на стороне Telegram). Для `/start`-приветствия
нужен запущенный `bot.js` (Node.js 18+):
```powershell
$env:BOT_TOKEN="ТОКЕН_ОТ_BOTFATHER"
$env:WEBAPP_URL="https://skysx67.github.io/gym-diary/"
node bot.js
```

## Локальная проверка приложения
```bash
python -m http.server 5177
```
Открыть `http://localhost:5177/index.html`. В обычном браузере работает `localStorage`;
синхронизация CloudStorage — только внутри Telegram.

## Формат кода тренировки (обмен)
Текст `"1" \n название \n (-|+)упражнение …`, где `-` — обычное упражнение, `+` — в суперсете
с предыдущим; всё это в base64url. Ограничения при разборе: до 60 упражнений, до 80 символов
на название. Импорт всегда выдаёт новые `id`.

Чтобы ссылка `https://t.me/dnevnik_spuchenka_bot?startapp=<код>` открывала приложение в один тап,
у бота нужно один раз включить Main Mini App: @BotFather → `/mybots` → бот → Bot Settings →
Configure Mini App. Без этого работает обмен кодом (он ни от чего не зависит).

## Идеи на будущее
Разбор текста программы из заметок через LLM-API (нужен бэкенд-прокси для ключа — уровень VPS);
объём (тоннаж), личные рекорды, экспорт.
